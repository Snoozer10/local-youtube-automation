# ============================================================
# FASTER-WHISPER TRANSCRIBE CONFIGURATION
# ============================================================

# --- Whisper Model Execution ---
# Options for size: tiny, base, small, medium, large-v3
WHISPER_MODEL_SIZE=small
WHISPER_LANGUAGE=ar
WHISPER_BEAM_SIZE=5
WHISPER_VAD_FILTER=true
WHISPER_MIN_SPEECH_DURATION_MS=250

# --- Prompt / Priming Control ---
# Slices script to max words to fit Whisper's strict 224-token prompt buffer
INITIAL_PROMPT_MAX_WORDS=120
DEFAULT_INITIAL_PROMPT=السلام عليكم، أهلاً بيكم. في الفيديو ده هنتكلم بالعامية المصرية، عشان نفهم الموضوع صح. زي الموبايل، التريند، الميكروباص، والسيستم. يا عم ركز معايا، وبلاش كلام كتير.

# --- Subtitle (.SRT) Cadence (High-Retention Kinetic Bursts) ---
# Keeps subtitles ultra-snappy for 1-second mobile reading
SUB_MAX_WORDS=4
SUB_TARGET_WORDS=2
SUB_MIN_GAP_SPLIT=0.35

# --- Image Visual Scene Pacing (image_timestamps.txt) ---
# High-Retention 2.5s to 4.5s Visual Metronome (Prevents static image boredom)
IMAGE_MIN_DURATION_SEC=2.2
IMAGE_TARGET_DURATION_SEC=3.5
IMAGE_MAX_DURATION_SEC=4.8
IMAGE_MIN_WORDS=5
IMAGE_MAX_WORDS=12
IMAGE_PAUSE_SPLIT_SEC=0.40

# --- Paths & File Preferences ---
RUNS_DIR=youtube_runs
POLISHED_AUDIO_SUBDIR=audacity_voice
AUDIO_FILENAME=full_episode_voice.wav
REFINED_SCRIPT_FILENAME=refined_script.txt
FINAL_OUTPUT_FILENAME=final_output.txt

# --- Export Controls ---
EXPORT_SRT=true
EXPORT_TIMELINE_TXT=true